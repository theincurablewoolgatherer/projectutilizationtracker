.modal-header
  button.close(type='button', ng-click='cancel()', aria-label='Close')
    span(aria-hidden='true') ×
  h4.modal-title {{user.username}} 
.modal-body()
    form(method = "post")
        .form-group
          label.control-label(for='user-type') Type
            select.form-control(data-ng-model = "user.usertype", data-ng-options = "o as o for o in usertypes")

         

.modal-footer
    button.btn.btn-default(type='button', ng-click='cancel()') Close
    button.btn.btn-primary(type='submit', ng-click='saveUser(user)') {{mode}}